{
    "version": "0.2.0",
    "configurations": [
        {
            "cwd": "${workspaceFolder}", // Обязательно указываем текущую рабочую дирректорию, чтобы сработали относительные пути в файле vars
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/wallet-service/main.go", // Указываем путь к файлу main.go
            "env": {
                "SERVICE_DEBUG": "true",
            },
            "args": [],
            "showLog": true
        }
    ]
}